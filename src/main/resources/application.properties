spring.application.name=works
# Don't change the port
# server.port = 9001
# server.address=0.0.0.0
# # for https
# Change the port to match your URL prefix
server.port=9173

# Ensure the app listens on all network interfaces
server.address=0.0.0.0

# (Optional) If you have Spring Security, permit Swagger
# springdoc.swagger-ui.path=/swagger-ui.html
server.forward-headers-strategy=framework 
#Database connectivity

#mysql connectivity
spring.datasource.url=jdbc:mysql://localhost:3306/sample?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver.class.name=com.mysql.cj.jdbc.Driver  

#Hibernate connectivity
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database.platform=org.hibernate.dialect.MySQL8Dialect
